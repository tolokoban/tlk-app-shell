/**
 * @module tfw.pointer-events
 *
 * @description
 * 
 *
 * @example
 * var mod = require('tfw.pointer-events');
 */


/**
 * @return void
 */
PointerEvents.prototype.on = function(events) {
    if (typeof events === 'function') {
        events = { tap: events };
    }


};
