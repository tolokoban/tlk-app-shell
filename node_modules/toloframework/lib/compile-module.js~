"use strict";

var Source = require("./source");

/**
 * Compile a javascriot module if needed.
 * @return `{ code: <string>, zip: <string> }`
 */
module.exports = function( prj, moduleName ) {
    var src = new Source( prj, "mod/" + moduleName + ".js" );

};
